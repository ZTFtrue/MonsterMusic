name: Release APKs

on:
  push:
    tags:
      - 'v*'
      - '[0-9]+.[0-9]+.[0-9]+*'
  workflow_dispatch:

jobs:
  build:
    name: Build & Publish Release APKs
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: gradle

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew

    - name: Decode Release Keystore
      if: env.SIGNING_KEY != ''
      env:
        SIGNING_KEY: ${{ secrets.MONSTER_MUSIC }}
      run: |
        echo "$SIGNING_KEY" | base64 --decode > release_keystore.jks

    - name: Build Release APKs
      env:
        KEYSTORE_PATH: ${{ github.workspace }}/release_keystore.jks
        KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
        KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
        KEY_STORE_PASSWORD: ${{ secrets.KEY_STORE_PASSWORD }}
      run: ./gradlew assembleRelease

    - name: Clean up Keystore
      if: always()
      run: rm -f release_keystore.jks

    - name: Prepare APKs with descriptive names
      run: |
        TAG_NAME="${{ github.ref_name }}"
        [ -z "$TAG_NAME" ] && TAG_NAME="latest"
        mkdir -p release-apks
        cp app/release/app-arm64-v8a-release.apk "release-apks/app-arm64-v8a-release.apk"
        cp app/release/app-armeabi-v7a-release.apk "release-apks/app-armeabi-v7a-release.apk"
        cp app/release/app-x86_64-release.apk "release-apks/app-x86_64-release.apk"
        cp app/release/app-x86-release.apk "release-apks/app-x86-release.apk"
        cp app/release/app-universal-release.apk "release-apks/app-universal-release.apk"
        cp app/release/app-release.apk "release-apks/app-release.apk"
        cp app/release/app-arm64-v8a-release.apk "release-apks/MonsterMusic-${TAG_NAME}-arm64-v8a.apk"
        cp app/release/app-armeabi-v7a-release.apk "release-apks/MonsterMusic-${TAG_NAME}-armeabi-v7a.apk"
        cp app/release/app-x86_64-release.apk "release-apks/MonsterMusic-${TAG_NAME}-x86_64.apk"
        cp app/release/app-x86-release.apk "release-apks/MonsterMusic-${TAG_NAME}-x86.apk"
        cp app/release/app-universal-release.apk "release-apks/MonsterMusic-${TAG_NAME}-universal.apk"
        ls -lh release-apks/

    - name: Upload Release Assets
      uses: softprops/action-gh-release@v2
      if: startsWith(github.ref, 'refs/tags/')
      with:
        files: |
          release-apks/*.apk
          app/build/outputs/mapping/release/mapping.txt
        generate_release_notes: true

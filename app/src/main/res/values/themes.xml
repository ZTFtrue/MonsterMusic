<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.MusicPitch" parent="android:Theme.Material.NoActionBar">
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="windowSplashScreenAnimationDuration">500</item>
        <item name="windowSplashScreenBackground">#00CCFF</item>
        <item name="postSplashScreenTheme">@style/UiTheme</item>
    </style>
    <!-- Base activity ui theme. -->
    <style name="UiTheme" parent="android:Theme.Material.NoActionBar"></style>

    <style name="Theme.MusicPitch.AppWidgetContainerParent" parent="@android:style/Theme.DeviceDefault">
        <!-- Radius of the outer bound of widgets to make the rounded corners -->
        <item name="appWidgetRadius">16dp</item>
        <!--
        Radius of the inner view's bound of widgets to make the rounded corners.
        It needs to be 8dp or less than the value of appWidgetRadius
        -->
        <item name="appWidgetInnerRadius">0dp</item>
    </style>

    <style name="Theme.MusicPitch.AppWidgetContainer" parent="Theme.MusicPitch.AppWidgetContainerParent">
        <!-- Apply padding to avoid the content of the widget colliding with the rounded corners -->
        <item name="appWidgetPadding">16dp</item>
    </style>

</resources>